中介者模式

    优点
    1：用中介对象封装一系列的对象交互，各对象不需要显示的相互引用
    2：对象与对象之间存在大量的关联关系，这样会导致系统的结构变的复杂，如果一个对象发生改变，
        需要跟踪与之相关联的对象，同时做出相应的处理
    3：减少了子类的生成

    缺点
    1：对象之间的连接导致对象可复用性低
    2：系统可扩展性低，系统维护困难

    使用场景
    1：系统中存在复杂的引用关系，它们之间的依赖关系结构混乱
    2：想通过中间类封装多个类中的行为，又不想生成太多的子类

